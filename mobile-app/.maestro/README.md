Run these flows against a debug build with Maestro installed.

`external-camera-simulation.yaml` assumes the app is already open on `MemberRecordScreen`.

Example:

```bash
maestro test mobile-app/.maestro/external-camera-simulation.yaml
```
