{
  "firestore": [
    {
      "database": "staging-db",
      "rules": "src/firebase/firestore.rules",
      "indexes": "firestore.indexes.json"
    },
    {
      "database": "prod-db",
      "rules": "src/firebase/firestore.rules",
      "indexes": "firestore.indexes.json"
    }
  ],
  "storage": [
    {
      "bucket": "gen-lang-client-0659584673-sv-firebase-staging",
      "rules": "src/firebase/storage.rules"
    },
    {
      "bucket": "gen-lang-client-0659584673-sv-firebase-prod",
      "rules": "src/firebase/storage.rules"
    }
  ],
  "functions": {
    "source": "functions",
    "predeploy": ["npm --prefix \"$RESOURCE_DIR\" run build"]
  }
}
